import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand colors, from the ExamBase logo
  static const Color primaryBlue = Color(0xFF1657E3);
  static const Color darkSlate = Color(0xFF2D3748);

  // Neutrals
  static const Color background = Color(0xFFF8F9FB);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1A202C);
  static const Color textSecondary = Color(0xFF718096);
  static const Color border = Color(0xFFE2E8F0);

  // Semantic
  static const Color success = Color(0xFF2F855A);
  static const Color error = Color(0xFFE53E3E);
  static const Color warning = Color(0xFFDD6B20);
}
